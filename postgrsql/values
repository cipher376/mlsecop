global:
  hosts:
    domain: mlsecops.local
  psql:
    host: gitlab-db-postgresql.ml-build.svc.cluster.local
    password:
      secret: gitlab-db-postgresql # Secret created by the standalone chart
      key: password
  redis:
    host: gitlab-redis-master.ml-build.svc.cluster.local

# THE MAGIC: Disable the bundled, "bad practice" versions
postgresql:
  install: false
redis:
  install: false
minio:
  install: false

gitlab:
  webservice:
    nodeSelector:
      workload: build-jobs
